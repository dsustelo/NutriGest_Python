#fazer auto-complete
#fazer eliminação de caracter

# sets e gets nas classes
Exemplo:
class Pessoa:
    def __init__(self, nome, idade):
        self.nome = nome
        self.idade = idade

    # getter
    @property
    def nome(self):
        return self._nome

    # setter
    @nome.setter
    def nome(self, nome):
        self._nome = nome.title()

# Comentários ao código
# ver as passagens de parametros/encapsulamento
#variáveis locais nos métodos
#usar variaveis ou evento do widget(exemplo, valor atual)

#renomear variáveis //excell
#renomear variáveis, métodos e classes em geral

#só colocar na db linhas preenchidas, retirar  if ingrediente is not 'none':
#usar grid em vez de place para listboxautocomplete?
#ver problema do one click na listboxautocomplete
#getters e setters
#mais classes, mais objetos, menos parametros
#rever only_numbers
#rever autocomplete
#modulo só para gui #modulos para db #modulos para classes #modulos para funções #modulos para validações #modulos para ficheiros etc
#cast nos tipos de dados
#nome nos pratos
#completar modelo de excell
#congruencia no nome das variáveis do inicioao fim do programa, passando por todas as funções
#aumentar ou reduzir templare consoant nr ingredientes
#reorganizar código e local das coisas

patch 1.1
-----------------------------------------------------------------------------------------------------------------------


#melhorar os grafismo da gui #figma? cursos youtube